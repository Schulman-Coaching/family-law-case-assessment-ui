# Family Law Case Assessment Tool

## Installation

```bash
# 1. Install dependencies
pip install spacy pandas streamlit

# 2. Download NLP model
python -m spacy download en_core_web_sm

# 3. Run command-line version
python case_assessment.py

# 4. Run web version (optional)
streamlit run case_assessment_app.py
```
